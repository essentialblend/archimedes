<!-- MainWindow.xaml -->

<Window x:Class="archimedes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:archimedes.Views"
        mc:Ignorable="d"
        Title="archimedes" Height="720" Width="1280">

  <Grid Background="{DynamicResource Brush.BG.Body}">
    <Grid.RowDefinitions>
      <RowDefinition Height="{DynamicResource Dim.BarHeight}"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Row 0: TRANSPORT BAR -->
    <views:TransportBar x:Name="TransportBar" Grid.Row="0"/>

    <!-- ROW 1: WORKSPACE -->
    <Border Grid.Row="1" Background="{DynamicResource Brush.BG.Panel}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="6"/>
          <ColumnDefinition Width="640" MinWidth="360"/>
        </Grid.ColumnDefinitions>

        <views:CodeEditor x:Name="CodeEditor" Grid.Column="0"/>

        <GridSplitter Grid.Column="1"
                      Width="6"
                      Background="{DynamicResource Brush.BG.Bar}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ResizeDirection="Columns"
                      ResizeBehavior="PreviousAndNext"
                      ShowsPreview="True"/>

        <Border Grid.Column="2"
                Background="{DynamicResource Brush.BG.Panel}"
                BorderBrush="{DynamicResource Brush.Border.Light}"
                BorderThickness="1,0,0,0">
          <Grid Margin="16,16,16,16">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <views:PerfVfxPanel x:Name="PerfVfxPanel"
                                Grid.Row="0"
                                Margin="0,0,0,16"/>

            <views:PerfVfxPanel x:Name="PerfVfxPanelMirror"
                                Grid.Row="1"
                                Margin="0,0,0,16"
                                IsEnabled="False"
                                Opacity="0.4"/>

            <Border Grid.Row="2"
                    Background="{DynamicResource Brush.BG.Panel}"
                    BorderBrush="{DynamicResource Brush.Border.Light}"
                    BorderThickness="1"
                    CornerRadius="8"
                    SnapsToDevicePixels="True"
                    UseLayoutRounding="True"
                    ClipToBounds="True">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="28"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{DynamicResource Brush.BG.Bar}"
                        Padding="8,4">
                  <TextBlock Text="SPECTRUM"
                             Foreground="{DynamicResource Brush.Text.Dim}"
                             FontFamily="Cascadia Mono"
                             FontSize="11"
                             VerticalAlignment="Center"/>
                </Border>

                <Border Grid.Row="1"
                        Margin="8"
                        Background="{DynamicResource Brush.BG.Body}"
                        BorderBrush="{DynamicResource Brush.Border.Light}"
                        BorderThickness="1"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                  <Grid>
                    <Rectangle Margin="6"
                               RadiusX="4"
                               RadiusY="4"
                               Fill="Transparent"
                               Stroke="{DynamicResource Brush.Acc.Vfx}"
                               StrokeThickness="1"
                               Opacity="0.2"/>
                    <Viewbox Margin="12" Stretch="Fill">
                      <Canvas Width="300" Height="80">
                        <Polyline Stroke="{DynamicResource Brush.Acc.Vfx}"
                                  StrokeThickness="1.4"
                                  Points="0,42 12,30 24,36 36,18 48,28 60,14 72,26 84,20 96,34 108,22 120,36 132,30 144,50 156,46 168,60 180,52 192,62 204,50 216,58 228,44 240,54 252,40 264,48 276,36 288,44 300,40"/>
                        <Polyline Stroke="{DynamicResource Brush.Acc.Vfx}"
                                  StrokeThickness="1"
                                  Opacity="0.5"
                                  Points="0,56 12,60 24,52 36,66 48,58 60,70 72,54 84,62 96,50 108,58 120,48 132,56 144,46 156,52 168,40 180,48 192,36 204,44 216,34 228,42 240,30 252,40 264,28 276,36 288,24 300,32"/>
                      </Canvas>
                    </Viewbox>
                  </Grid>
                </Border>
              </Grid>
            </Border>
          </Grid>
        </Border>
      </Grid>
    </Border>
  </Grid>
</Window>
